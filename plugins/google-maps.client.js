// plugins/google-maps.client.js
window.initMap = function () {
  console.log('Google Maps API carregada com sucesso!');
  // Adicione aqui qualquer lógica adicional para inicializar mapas
};

export default defineNuxtPlugin(() => {
  // O plugin em si pode ser usado para lógica adicional, mas a definição já está no escopo global
});